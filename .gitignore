__pycache__/
*.pyc
.venv/
venv/
.env
*.egg-info/
.DS_Store
out/

# Raw client data pack: contains customer names/cities and free-text
# messages. Not committed to the (public) repo -- see data/README.txt
# for how to obtain it.
data/*.csv
data/*.pdf

# Scratch dumps used while manually verifying the eval -- contain raw
# customer message text, same PII concern as the data pack above.
eval_narrative_sample.json
eval_sample_messages.json
